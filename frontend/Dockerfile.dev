# Development Dockerfile for Angular with hot reload
FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Expose Angular dev server port
EXPOSE 4200

# Run Angular dev server with host set to 0.0.0.0 to allow external connections
# --poll flag enables file watching in Docker environment
CMD ["npm", "run", "start", "--", "--host", "0.0.0.0", "--poll", "2000", "--disable-host-check"]
